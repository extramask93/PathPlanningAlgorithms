FILE(COPY whitemap40x40.csv DESTINATION "${CMAKE_BINARY_DIR}")
FILE(COPY whitemap80x80.csv DESTINATION "${CMAKE_BINARY_DIR}")
FILE(COPY whitemap160x160.csv DESTINATION "${CMAKE_BINARY_DIR}")
FILE(COPY whitemap20x20.csv DESTINATION "${CMAKE_BINARY_DIR}")
FILE(COPY map-warehouse.csv DESTINATION "${CMAKE_BINARY_DIR}")
